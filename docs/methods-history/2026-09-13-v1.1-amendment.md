# Prospective amendment: synthetic disclosure demonstration

Date: September 13, 2026. Methods version: 1.0 → 1.1.

**Data access:** only generated synthetic fixtures had been viewed. No participant responses had been collected or analyzed.

**Reason:** the initial demo exempted synthetic data from small-cell suppression. Although the exemption was documented, it prevented portfolio viewers from seeing the release controls in action and made the demonstration inconsistent with participant-run behavior.

**Change:** apply identical sample-size and outcome-cell release rules to synthetic and participant runs. Engineer the default synthetic outcome and grouping values to show both available tables and a withheld campus table. Source selections remain random. Display the withholding reason directly instead of replacing the campus panel with the overall distribution. Add a GitHub link and clarify the random source values beside the source chart.

**Effect on comparability:** participant eligibility, exclusions, denominators, and release thresholds are unchanged. Synthetic satisfaction and research-time values change and must not be compared as research findings. The default QA accounting remains 60 raw, 50 retained, and ten excluded rows.

**Version preservation:** [the original version 1.0 lock](2026-09-13-v1-lock.json) is preserved byte for byte and authenticates the initial code available at commit `fd77566dabe1bde6c397cc12398c84785e16ca91`. The root `methods-lock.json` records the amended baseline; it is still a pre-collection development baseline, not a completed production launch freeze. Finish the pilot and launch record and create a separately named production lock before recruitment.
